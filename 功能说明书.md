# AIBook 功能说明书

> **AIBook**（项目标识 `ai-book`）是 macOS 原生辅助读书应用：**左页**阅读与编辑本地文本，**右页**通过 AI 完成讲解、翻译、朗读与（可选）应用自我升级。  
> **适用系统**：macOS 13 及以上。  
> **产品定位**：辅助读书——帮助理解字面含义、上下文、译文对照与沉浸阅读，而不是替代完整电子书阅读器。

应用内说明书与仓库根目录 `功能说明书.md` 内容一致；修改后请同步 `Sources/AIBook/Resources/功能说明书.md` 以便重新打包。

---

## 在应用内如何打开

- 菜单栏 **帮助 → AIBook 功能说明书**（应用内窗口，可选中复制文字）
- **帮助 → 在访达中显示说明书**（用系统默认 Markdown 编辑器打开）
- **book 设置** / **进化设置** 顶栏 **「说明书」** 按钮

---

## 1. 产品概述

### 1.1 您可以用 AIBook 做什么

| 能力 | 说明 |
|------|------|
| **AI 讲解** | 针对左页选中或全文，用中文解释含义、背景与难点 |
| **AI 翻译** | 逐字拆解或整段通译，结果可编辑、保存，支持表格对照 |
| **名著补全** | 根据节选识别名著，补全为风格一致的完整篇章 |
| **语音朗读** | 原文、讲解、翻译均可朗读，支持本地与在线神经女声 |
| **沉浸阅读** | 全屏双页/单页，仿真翻书动画，无 AI 界面干扰 |
| **目录书库** | 记住多个文件夹，快速打开原文、翻译或讲解文稿 |
| **自我进化** | （高级）AI 分析应用源码、排队优化项并自动改码升级 |

### 1.2 界面隐喻

整体 UI 采用「**翻开的书**」视觉：皮革封面、纸张纹理、书脊丝带、页码装饰。默认浅色书桌风格，可在 book 设置中切换预设主题。

### 1.3 两种顶层模式

| 模式 | 何时使用 |
|------|----------|
| **学习模式** | 需要 AI 讲解、翻译、朗读、编辑文稿 |
| **阅读模式** | 只想连续读正文，像读实体书一样翻页 |

二者可随时切换，**阅读进度**（spread 索引）按文件单独记忆。

---

## 2. 主窗口结构（学习模式）

```
┌─────────────────────────────────────────────────────────────┐
│ 顶栏：品牌 · 文件名 · 状态胶囊 · 大模型/Cursor · 播控        │
│ 工具栏：原文操作 | 讲解操作 | 翻译操作 | 朗读 | 进化…         │
├──────────────────────────┬──────────────────────────────────┤
│  — 左页 —                 │  — 右页 —                         │
│  可编辑原文               │  Tab：读书助手 | AI进化             │
│  （或命令笔记）           │  · 讲解结果 / 翻译结果 分栏         │
│                           │  · 对话流、输入框、翻译工具条       │
└──────────────────────────┴──────────────────────────────────┘
```

- **默认窗口**：约 1280×860，最小约 960×640。
- **顶栏胶囊**：当前文件名、未保存提示、当前 AI 后端（大模型 API / Cursor 本地）、book 或 Cursor 模型名、进化队列状态等。
- **右页 Tab**：
  - **读书助手**：讲解对话 + 翻译区（两套内容独立）。
  - **AI 进化**：优化队列、Agent 执行过程、进化命令管理。

### 2.1 学习模式布局切换

| 布局 | 操作 |
|------|------|
| **双页** | 左原文 + 右 AI 并排 |
| **左全屏** | 仅左页占满，便于编辑长文 |
| **右全屏** | 仅右页占满，便于阅读长讲解或翻译 |
| **恢复双页** | `Esc`（在非阅读模式时） |

顶栏或视图菜单中可切换 **⌘⌥1 / ⌘⌥2 / ⌘⌥3**（双页 / 左全屏 / 右全屏，以应用内菜单为准）。

### 2.2 读书助手右页分栏

在「读书助手」Tab 内，右页再分为：

| 分栏 | 内容 |
|------|------|
| **讲解结果** | 多轮讲解对话、流式输出、可保存/打开/清空 |
| **翻译结果** | 翻译 JSON 或表格对照、可编辑、对齐与锁定 |

顶栏胶囊 **「讲解」/「翻译」** 可切换分栏；执行翻译类操作时会自动切到翻译分栏。

### 2.3 专注与界面精简

翻译/讲解区支持隐藏部分 chrome（如工具栏、分栏切换），只保留正文，便于长时间阅读 AI 输出。具体开关见右页翻译/讲解区顶部的显示控制。

---

## 3. 阅读模式（沉浸翻书）

### 3.1 进入与退出

| 操作 | 说明 |
|------|------|
| 进入 | 顶栏 **「阅读模式」**，快捷键 **⌘⇧P**（需已打开非空正文） |
| 退出 | 顶栏 **「学习模式」** 或 **Esc** |

阅读模式下**不提供** AI 讲解与翻译；需要 AI 时请先返回学习模式。

### 3.2 布局

| 选项 | 说明 |
|------|------|
| **双页** | 左第 N 页、右第 N+1 页，中间 **书脊固定不动** |
| **全屏** | 单页占满窗口 |
| 切换 | 阅读顶栏 **双页 / 全屏**，或 **⌘⌥1 / ⌘⌥2** |

### 3.3 翻页方式

| 方式 | 行为 |
|------|------|
| **← / →** | 上一跨页 / 下一跨页 |
| **点击左页 / 右页** | 向后 / 向前翻（双页布局下） |
| **水平滑动** | 跟手翻页；超过约三分之一松手则翻完，否则回弹 |
| **顶栏箭头** | 与键盘等效 |

翻页动画为 **单张纸页** 自然卷曲，书脊不参与变形，避免「撕书」感。

### 3.4 分页与进度

- 分页依据与左页相同的 **字号、字体、行距、页边距**（book 设置 → 外观风格）。
- 窗口大小或排版变化时会 **重新分页**，并尽量保持当前阅读位置。
- 每个文件的 **spread 索引** 会保存；再次进入阅读模式时恢复上次位置。
- 页脚显示类似：**第 3–4 页 / 共 128 页**（双页）或 **第 N 页 / 共 M 页**（全屏）。

---

## 4. 文稿与文件管理

### 4.1 支持的文稿类型

- 主要支持 **UTF-8 纯文本 `.txt`**。
- 左页全文可 **自由编辑**；支持选中、复制、拖拽打开文件。

### 4.2 文件菜单与原文操作

| 功能 | 快捷键 | 说明 |
|------|--------|------|
| 新建 | ⌘N | 清空并新建左页文档 |
| 打开文本 | ⌘O | 选择本地 `.txt` |
| 打开目录 | 文件菜单 | 打开目录浏览面板（见 4.4） |
| 保存 | ⌘S | 保存到当前文件路径 |
| 另存为 | ⌘⇧S | 左页全文存为新文件 |
| 导出选中 | ⌘⌥S | 仅导出左页当前选区 |
| 全选左页 | ⌘⌃A | |
| 名著补充 | ⌘⇧C | 见第 7 节 |

未打开外部文件时，左页加载 **命令笔记**（见 4.3）。

### 4.3 命令笔记（默认左页）

- 首次使用或无打开文件时，左页显示命令笔记，用于记录说明或历史进化相关文本。
- 保存位置：`~/Library/Application Support/AIBook/readme-notes.txt`
- 若无本地副本，会回退到应用内置或项目中的 `readme.txt` 模板。
- **⌘S** 保存命令笔记到 Application Support。

> 当前版本的 **优化队列** 以 `optimization-queue.json` 为主；命令笔记不再作为进化运行时命令源，但可作个人备忘。

### 4.4 目录浏览（书库）

**入口**：**文件 → 打开目录…**

| 步骤 | 说明 |
|------|------|
| 添加目录 | 侧边栏 **+**，选择书籍根文件夹 |
| 切换目录 | 在「已打开目录」列表中点选 |
| 浏览文件 | 中间树形展开子文件夹 |
| 选择打开目标 | **原文** → 左页；**翻译** → 右页翻译分栏；**讲解** → 右页讲解分栏 |
| 打开 | 双击或底部确认 |

已添加的目录与展开状态会 **持久化**，下次启动仍可用。适合在同一书库的多本 `.txt`、`-翻译.txt`、讲解稿之间切换。

---

## 5. AI 后端与配置

讲解、翻译、名著补充共用 **读书大模型（book 配置）**；AI 进化使用 **进化设置** 中的 Cursor 或大模型（可与 book 分开配置）。

### 5.1 两种解析后端（顶栏切换）

| 后端 | 适用场景 |
|------|----------|
| **大模型 API** | OpenAI、DeepSeek、Ollama、通义、智谱等 OpenAI 兼容接口 |
| **Cursor 本地** | 本机已配置 Cursor API，通过 Node 桥接调用 Agent |

切换前应用会检查配置是否完整；未完成配置时相关按钮会提示先打开 **book 设置**。

### 5.2 book 设置（读书助手）

**入口**：顶栏 **book 设置** 按钮。

#### AI 模型设置

- 按 **提供商** 分别保存 API 地址、Key、默认模型。
- 内置提供商示例：

| 提供商 | 典型用途 |
|--------|----------|
| OpenAI | 云端 GPT 系列 |
| DeepSeek | 高性价比中文 |
| Ollama | **本机免费**（推荐日常读书） |
| Moonshot / 通义 / 智谱 | 国内云端 |
| 自定义 | 任意 OpenAI 兼容端点 |

**Ollama 快速开始**：

```bash
ollama serve
ollama pull llama3.2
```

在 book 设置中选择 Ollama，可 **扫描本机已安装模型** 并选用。若 Ollama 启用了 API Key，请在设置中填写。

- **上下文占比**：控制每次请求带入的左页原文节选比例（避免超出模型窗口）。

#### 语音设置

见第 8 节。

#### 外观风格

见第 9 节。

### 5.3 进化设置

**入口**：菜单 **AIBook → 设置**（进化 Tab）。

| 配置项 | 说明 |
|--------|------|
| Cursor API Key | 或环境变量 `CURSOR_API_KEY` |
| Cursor 模型 | 如 Composer 系列 |
| cursor-bridge 路径 | 项目内 `cursor-bridge` 目录 |
| 自动升级链 | 一次点击「进化」后，若队列仍有待办，可连续执行（见第 10 节） |

**Cursor 本地依赖**：

```bash
cd ai-book/cursor-bridge
npm install
```

需 **Node.js 18+**。

---

## 6. 讲解功能（读书助手 · 讲解结果）

### 6.1 基本流程

1. 在左页 **选中** 一段文字（或不选，使用全文上下文）。
2. 点击 **「选择讲解」**（⌘R）或 **「全文讲解」**（⌘⇧R），或顶栏 **讲解操作** 菜单。
3. AI **流式** 输出到右页「讲解结果」分栏。
4. 讲解完成后，默认可 **自动朗读**（可在流程中停止；语音见第 8 节）。

讲解角色设定为：**清晰中文**，解释字面含义、上下文、重点与难点，条理简洁。

### 6.2 多轮追问

- 右页底部 **输入框** 可继续提问，AI 会结合当前文件与历史对话回答。
- 同一本书的对话会在重启后 **尽量恢复**（按文件路径关联会话存储）。

### 6.3 讲解文稿管理（讲解操作菜单）

| 操作 | 说明 |
|------|------|
| 新建 | 清空当前讲解对话 |
| 打开 | 从 `.txt` 载入讲解稿到右页 |
| 保存 | 将讲解内容保存为 UTF-8 文本 |
| 全选 | 选中讲解区全部文字 |
| 清空讲解 | 删除当前讲解对话 |
| 朗读讲解 | 朗读讲解全文或停止 |

### 6.4 单条消息操作

在讲解对话气泡上 **右键**，可复制、删除单条消息，或对 **单条 AI 回复** 发起朗读（视当前版本菜单为准）。

### 6.5 与选区的关系

点击顶栏按钮时，系统可能短暂清除左页高亮；应用会 **缓存最近一次非空选区**，保证「选择讲解」仍针对您刚才选中的文字。

---

## 7. 翻译功能（读书助手 · 翻译结果）

### 7.1 两种翻译模式

| 模式 | 入口 | 输出特点 |
|------|------|----------|
| **逐字翻译** | 翻译操作 → 逐字翻译 | 按原文顺序拆解字/词，附简短说明，适合精读、词汇对照 |
| **整段翻译** | 翻译操作 → 整段翻译 | 保留段落结构，通顺完整，适合章节阅读 |

**翻译源文本**：优先左页 **当前选中**；无选中则使用左页 **全文**。

生成结果为结构化 **JSON**（内含对齐块），在右页翻译区展示；您可 **直接编辑** 文本或表格。

### 7.2 翻译文件管理

| 操作 | 说明 |
|------|------|
| 打开翻译 | 载入已有翻译 JSON/文本 |
| 保存翻译 | 常见命名：`原文件名-翻译.txt` |
| 清空翻译 | 删除当前翻译内容与对齐 |
| 全选翻译 | ⌘⌃⇧A |
| 朗读翻译 | ⌘⌥T 或翻译工具条「朗读」 |

### 7.3 表格对照与对齐

当翻译 JSON 包含有效 **对齐块**（字/词/段等级）时：

| 功能 | 说明 |
|------|------|
| **表格对照** 开关 | 右页以 **原文–译文对照表** 展示，便于逐行查阅 |
| **对齐原文** | 左页正文变更后，重新计算与译文的对齐关系 |
| **锁定对照** | 锁定后防止误触整体对齐；单行编辑通常仍可用 |
| **解锁对照** | 恢复可整体对齐 |

若左页改动较大，对齐可能 **失效**，翻译工具条会提示 **「对齐已失效」**，需重新「对齐原文」。

### 7.4 与左页滚动的联动

在学习模式 **双页** 布局下，开启 **表格对照** 且对齐有效时，左页滚动可与对照表 **联动高亮** 当前段落/块，便于对照阅读（具体以当前版本为准）。

---

## 8. 语音朗读

### 8.1 朗读入口汇总

| 对象 | 常见入口 |
|------|----------|
| 左页选中 | 朗读功能 → 朗读原文选中；⌘⌥R（选中或全文策略见菜单） |
| 左页全文 | 朗读功能 → 朗读原文全文 |
| 翻译 | ⌘⌥T、翻译工具条、翻译操作菜单 |
| 讲解 | 讲解操作 → 朗读讲解；单条消息右键 |
| 试听 | book 设置 → 语音设置 → 试听当前声音 |

### 8.2 顶栏播控

朗读进行中时，顶栏中部显示 **暂停 / 继续 / 停止**，适用于当前正在播放的讲解或原文，无需进入子菜单。

### 8.3 语音引擎（book 设置 → 语音设置）

| 模式 | 特点 |
|------|------|
| **极速（本地）** | macOS 系统语音，首句延迟最低 |
| **平衡（在线）** | Edge TTS，24kHz / 48kbps |
| **高音质（在线）** | Edge TTS，更高码率 |
| **美声（在线）** | 高码率 + 较慢咬字 + 韵律增强（默认倾向） |

### 8.4 语言处理模式

| 模式 | 说明 |
|------|------|
| 高效 | 较少改写，长句切分少，速度优先 |
| 平衡 | 速度与清晰度均衡 |
| 深度 | 更短分句、标点停顿、多音字处理（默认倾向） |

### 8.5 讲解声音

- **系统中文语音**：使用 macOS 内置中文声音。
- **在线神经女声**（Edge TTS）：如晓伊、晓晓及多种方言/地区女声。
- 在线声音需先在设置中 **下载样本** 到本地缓存目录后方可稳定使用。

朗读前会对 Markdown 等做 **清洗与分句**，以提升听感。

---

## 9. 名著补充

| 项目 | 说明 |
|------|------|
| 入口 | 顶栏 **名著补充**、原文操作菜单、⌘⇧C |
| 输入 | 左页节选（选中或全文） |
| 行为 | AI 识别所属名著， **补全为连贯完整篇章**，尽量保持原著文风 |
| 输出 | 写回 **左页**（替换选中或全文），并尝试 **自动保存** |

适合从教材摘录、断章残篇恢复可读上下文。**请勿**用于侵犯版权的非法复制；请仅对您有权使用的文本使用本功能。

---

## 10. AI 进化（高级功能）

> **警告**：进化会修改 AIBook 源码并重新安装应用。请在了解 Git/备份的前提下使用，不建议在重要生产环境盲目开启自动升级链。

### 10.1 做什么

- **分析优化**：AI **只读** 浏览项目，提出可改进项，写入 **优化队列**（不改代码）。
- **进化**：从队列取一条 **待办**，AI **改代码** → 构建安装 → **重启应用**。

### 10.2 典型流程

1. 切换到右页 **AI 进化** Tab。
2. 点击 **「分析优化」**，等待 Agent 输出建议并入队。
3. 查看队列中的条目（标题、编号、状态）。
4. 点击 **「进化」**（⌘E）执行 **下一条 pending**。
5. 观察 **思考过程** 与 **工具步骤** 时间线。
6. 完成后应用可能自动重启；若开启 **自动升级链** 且您本次会话已手动触发过进化，可能 **连续执行** 后续待办。

### 10.3 进化命令与队列文件

- 队列文件：`~/Library/Application Support/AIBook/optimization-queue.json`
- 进化 Tab 内可 **保存/打开** 进化命令文稿（UTF-8）。
- 分析入队时会 **去重**（标题规范化匹配）；单次分析建议条目数有上限（约 8 条）。

### 10.4 双通道执行

| 通道 | 说明 |
|------|------|
| **Cursor 本地** | 完整 Agent 工具链，适合复杂改码 |
| **大模型 API** | 工具调用 + 沙箱内读写，Cursor 不可用时回退 |

### 10.5 Token 与状态

- 进化与长对话受 **Token 预算** 限制，界面会显示用量；超限将阻止发送。
- 顶栏胶囊可显示 **待办条数**、下一条编号等状态。

---

## 11. 外观与排版（book 设置 → 外观风格）

| 项目 | 影响范围 |
|------|----------|
| **预设主题** | 皮革色、纸张色、按钮与装饰风格 |
| **正文字号** | 左页、阅读模式分页、翻页快照 |
| **字体族** | 中文阅读字体选择 |

修改后 **阅读模式会重新分页**；学习模式左页即时生效。阅读模式翻页动画中的纸页纹理与当前主题一致。

---

## 12. 菜单栏与系统菜单补充

除顶栏工具按钮外，**文件 / 编辑** 等系统菜单聚合了常用快捷键（与顶栏「原文操作」「讲解操作」等一致）。  
**帮助** 菜单提供本说明书与访达定位。

**AIBook → 设置**：进化相关。  
**book 设置**（顶栏）：读书、翻译、语音、外观。

---

## 13. 快捷键总表

| 功能 | 快捷键 |
|------|--------|
| 新建 | ⌘N |
| 打开 | ⌘O |
| 保存 | ⌘S |
| 另存为 | ⌘⇧S |
| 导出左页选中 | ⌘⌥S |
| 讲解选中 | ⌘R |
| 讲解全文 | ⌘⇧R |
| 朗读原文（菜单定义：选中/全文） | ⌘⌥R |
| 朗读翻译全文 | ⌘⌥T |
| 名著补充 | ⌘⇧C |
| 进化 | ⌘E |
| 进入阅读模式 | ⌘⇧P |
| 阅读：上一页 / 下一页 | ← / → |
| 阅读：双页 / 全屏 | ⌘⌥1 / ⌘⌥2 |
| 退出阅读 / 恢复双页 | Esc |
| 全选左页 | ⌘⌃A |
| 全选翻译 | ⌘⌃⇧A |

> 部分布局快捷键以应用内 **显示** 菜单为准；悬停顶栏按钮可查看完整 hint。

---

## 14. 数据与隐私

### 14.1 本地存储（Application Support）

根目录：`~/Library/Application Support/AIBook/`

| 内容 | 路径/说明 |
|------|-----------|
| 命令笔记 | `readme-notes.txt` |
| 优化队列 | `optimization-queue.json` |
| 对话会话 | 按书籍路径缓存 |
| 目录浏览 | 已打开根目录与展开状态 |
| 在线语音样本 | `voices/` 子目录 |
| 自动升级链状态 | `auto-evolution-active` 等 |

### 14.2 网络与密钥

- **大模型 / Cursor** 请求会发送到您配置的 API 或 Cursor 服务；Key 保存在本机偏好设置中，请勿分享设备。
- **Edge TTS** 在线朗读需要网络；本地系统语音可离线。

---

## 15. 安装与更新

### 15.1 安装到应用程序文件夹

```bash
cd ai-book
./scripts/build-and-install.sh
```

### 15.2 开发运行

```bash
swift run
```

### 15.3 示例文本

`Sources/AIBook/Resources/sample.txt` 可用于首次体验打开与讲解。

---

## 16. 推荐使用场景

### 16.1 日常读书 + 答疑

1. ⌘O 打开章节 `.txt`。  
2. 选中不懂的段落 → ⌘R 讲解。  
3. 底部输入框追问。  
4. 需要专注阅读时 → ⌘⇧P 进入阅读模式。

### 16.2 外语 / 经典精读

1. 左页载入原文。  
2. **整段翻译** 或 **逐字翻译**。  
3. 开启 **表格对照**，**锁定** 稳定版本。  
4. ⌘⌥T 朗读译文，⌘⌥R 朗读原文对比。

### 16.3 多文件书库

1. **文件 → 打开目录…** 添加书库根目录。  
2. 树中选择 `chapter-01.txt` → 打开到 **原文**。  
3. 选择 `chapter-01-翻译.txt` → 打开到 **翻译**。  
4. 在同一窗口切换讲解与翻译分栏。

### 16.4 节选补全

1. 左页粘贴教材节选。  
2. ⌘⇧C 名著补充。  
3. 检查补全结果后 ⌘S 保存。

---

## 17. 常见问题

**Q：点击讲解提示未配置大模型？**  
A：打开 **book 设置 → AI 模型设置**，选择提供商并填写 API Key；本机推荐 Ollama。

**Q：Cursor 本地不可用？**  
A：检查 Node.js、cursor-bridge 的 `npm install`、Cursor API Key 与进化设置中的路径。

**Q：翻译表格对照是灰的？**  
A：需先完成逐字/整段翻译且 JSON 含有效对齐块；对齐失效时需「对齐原文」。

**Q：阅读模式页码变了？**  
A：更改字号或窗口大小会重新分页；属正常现象，进度仍按 spread 尽量恢复。

**Q：进化后应用行为变了？**  
A：进化修改的是本地安装的 AIBook 副本；请用 Git 管理源码并谨慎使用自动升级链。

**Q：说明书与界面不一致？**  
A：以您安装的 **AIBook 版本** 为准；悬停按钮查看最新 hint。

---

## 18. 版本与文档

- 本说明书随应用 Bundle 分发，可在 **帮助** 菜单打开。  
- 工程内另有设计文档（阅读模式、UI 风格、进化设计等），供开发与二次定制参考。  
- 项目 README 含 Ollama、Cursor 安装的简明步骤。

---

**感谢使用 AIBook · 辅助读书。**
