# AIBook 整体 UI 文档设计

> 文档版本：基于当前 SwiftUI 源码梳理  
> 关联文档：[整体功能文档设计.md](./整体功能文档设计.md)

---

## 1. 设计愿景与风格定位

### 1.1 视觉隐喻

AIBook 将 macOS 应用包装为**书桌上的打开的书**：深色木质桌面、台灯暖光、皮革封面顶栏、左右纸页、书脊丝带书签、页角折痕与页码装饰。这一隐喻与「辅助读书」产品定位高度一致，营造沉浸、安静、偏古典的阅读氛围。

### 1.2 设计系统（BookTheme）

| 维度 | 规范 | 说明 |
|------|------|------|
| **色彩** | 皮革棕、宣纸米黄、墨色文字、金色点缀、朱红警示 | 左页略亮、右页略暖，区分阅读区与 AI 区 |
| **字体** | 宋体 SC 为主（标题 22 / 正文 16 / 标签 13 / 注释 12） | 左页 NSTextView 用 17pt 宋体 + 大行距，利于长文阅读 |
| **圆角** | 顶栏 18、书页 8、卡片 10–16、按钮胶囊 | 统一 continuous 圆角 |
| **按钮** | `BookActionButton`（顶栏深色）、`BookPageActionButton`（纸面浅色） | 悬停动画 0.16s，主操作用金色渐变 |
| **状态** | `BookStatusPill` 胶囊标签 | 展示模式、未保存、进化状态等 |

### 1.3 窗口规范

- 默认 1280×860，最小 960×640
- 隐藏系统标题栏，`.windowToolbarStyle(.unified)`
- 强制浅色模式（与纸本书籍一致）
- 外层 24pt 边距，书桌渐变 + 径向台灯光

---

## 2. 信息架构总览

```
AIBook 主窗口
├── 顶栏（bookHeader）
│   ├── 品牌区：Logo + 名称 + 状态胶囊 + 文件名
│   ├── 模式区：当前 AI 后端胶囊 + 未保存 + 大模型/Cursor 切换
│   └── 工具栏（三等分）
│       ├── 左：文档（打开/保存/另存为/名著补充）
│       ├── 中：读书助手 Tab +「更多」菜单（讲解/朗读/翻译…）
│       └── 右：AI进化 Tab +「更多」菜单（进化/设置/停止）
├── 书页区（openBook）
│   ├── 左页：原文 / 命令笔记（SelectableTextView）
│   └── 右页：ExplanationChatView
│       ├── Tab A：读书助手
│       │   ├── 配置提示条（未配置时）
│       │   ├── 翻译编辑区（lessonPlanEditor）  ← 主内容区
│       │   └── 输入区（LLM/Cursor Composer）
│       └── Tab B：AI进化
│           ├── 配置提示条
│           ├── 进化工具面板（进化/队列 Tab）
│           ├── 重建 Banner（打包时）
│           ├── 对话列表（chatList）            ← 主内容区
│           └── 输入区（LLM/Cursor Composer）
└── 模态：SettingsView（Sheet）
    ├── AI模型设置
    └── 语音设置
```

---

## 3. 界面分区详述

### 3.1 顶栏品牌区

**组成**：书本图标、AIBook 标题、`ai-book` / `辅助读书` / 平台标签胶囊、进化状态胶囊（有队列时）、多模型胶囊（LLM 模式时）、当前文件名。

**合理性评估**：⭐⭐⭐⭐ **较合理**

| 优点 | 问题 |
|------|------|
| 一眼识别产品与当前文件 | 胶囊过多时窄屏会拥挤 |
| 进化队列状态置于顶栏便于感知自动升级 | `SwiftPlatform.implementationLabel` 对普通用户价值低 |
| Help 提示承载产品定位说明 | 文件名与多枚胶囊同一行，信息密度偏高 |

**建议**：窄窗口下折叠次要胶囊；开发者向标签可移至关于页。

---

### 3.2 顶栏 AI 后端切换

**组成**：当前后端状态胶囊 +「大模型 API」「Cursor 本地」两个切换按钮。

**行为**：点击切换会**自动跳到 AI 进化 Tab**（`selectRightPageTab(.aiEvolution)`），并更新 `explanationSource`。

**合理性评估**：⭐⭐ **部分合理，存在明显问题**

| 优点 | 问题 |
|------|------|
| 全局一处切换，讲解/进化/翻译共用后端 | **切换后端强制跳转 AI 进化 Tab**，打断读书流程 |
| 运行中禁用切换，避免状态错乱 | 顶栏与进化面板内均有后端切换，**重复入口** |
| 胶囊显示当前模型名（LLM 已配置时） | 读书用户可能不理解「进化区」与「讲解」共用设置 |

**建议**：
1. 切换后端时不强制跳转 Tab，或仅在进化 Tab 显示切换器。
2. 读书助手区增加轻量「当前：DeepSeek · xxx」只读提示。

---

### 3.3 顶栏工具栏 — 文档区（左）

| 控件 | 功能 | 评估 |
|------|------|------|
| 打开 | 打开 `.txt` | ⭐⭐⭐⭐⭐ 合理，核心入口 |
| 保存 | 保存左页 / 命令笔记 | ⭐⭐⭐⭐⭐ 合理 |
| 另存为 | 全文另存 | ⭐⭐⭐⭐ 合理，略低频可保留 |
| 名著补充 | AI 补全名著节选 | ⭐⭐⭐⭐ 合理，与读书场景契合；放顶栏略隐蔽但可接受 |

---

### 3.4 顶栏工具栏 — 读书助手区（中）

| 控件 | 功能 | 评估 |
|------|------|------|
| 读书助手 Tab | 切换右页 Tab | ⭐⭐⭐⭐⭐ 合理 |
| 「更多」菜单 | 聚合讲解/朗读/翻译等 | ⭐⭐⭐ **部分合理** |

**「更多」菜单内容**：

| 菜单项 | 评估 | 说明 |
|--------|------|------|
| 讲解 | ⭐⭐⭐⭐⭐ | 核心功能；藏在二级菜单降低可发现性 |
| 朗读 | ⭐⭐⭐⭐ | 合理；与讲解同属阅读流 |
| 逐字翻译 / 整段翻译 | ⭐⭐⭐ | 功能合理但**入口过深**（三级：更多→翻译） |
| 朗读翻译 / 全选翻译 | ⭐⭐⭐⭐ | 与翻译区联动合理 |
| 打开/保存/清空翻译 | ⭐⭐⭐ | 与右页翻译区功能重复，属冗余入口 |
| 停止 | ⭐⭐⭐⭐⭐ | 必要 |

**建议**：将「讲解」提升为顶栏一级按钮（与产品设计一致）；翻译可保留在菜单或右页内工具条。

---

### 3.5 顶栏工具栏 — 实用区（右）

| 控件 | 功能 | 评估 |
|------|------|------|
| AI进化 Tab | 切换右页 | ⭐⭐⭐⭐⭐ 合理 |
| 进化 | 执行下一条命令 | ⭐⭐⭐⭐ 合理；与进化面板内按钮重复 |
| 设置 | 打开 Settings Sheet | ⭐⭐⭐ **部分合理** — `openSettings()` 会先切到 AI 进化 Tab |
| 停止 | 中止任务 | ⭐⭐⭐⭐⭐ 合理 |

---

### 3.6 左页 — 原文 / 命令笔记

**组成**：
- 页眉：标题 + 快捷键提示 subtitle
- 正文：`SelectableTextView`（宋体、大行距、CJK IME 优化）
- 空态：`EmptyPageWelcome`（翻开书页引导 + 打开文件按钮）
- 页脚：页码标记、字数、选中字数、全选、保存状态
- 支持拖拽 `.txt` 打开

**合理性评估**：⭐⭐⭐⭐⭐ **非常合理**

| 优点 | 问题 |
|------|------|
| 阅读排版舒适，IME 处理专业 | 命令笔记与原文共用左页，新用户可能混淆两种用途 |
| 页脚实时字数/选中反馈清晰 | subtitle 快捷键文案较长，小屏可能截断 |
| 空态引导明确 | — |
| 命令笔记自动保存体验好 | — |

**建议**：左页页眉可增加模式提示（「当前：命令笔记」vs「当前：xxx.txt」），强化双用途认知。

---

### 3.7 右页 — 读书助手 Tab

**实际布局**（`readingAssistantPage`）：

```
配置提示条（可选）
翻译编辑区 lessonPlanEditor（占满主区域）
Composer 输入框（底部）
```

**关键发现**：`chatList`（对话气泡列表）**仅存在于 AI 进化 Tab**，读书助手 Tab **不展示讲解对话历史**。

**合理性评估**：⭐⭐ **存在结构性缺陷**

| 设计意图（推测） | 实际问题 |
|------------------|----------|
| 右页主区留给「翻译」编辑 | **讲解结果无可见区域**，用户只能靠朗读感知 AI 回复 |
| 底部 Composer 支持追问 | 有输入框但**看不到历史消息**，追问上下文不直观 |
| 欢迎消息写入 `readingPromptMessages` | 欢迎语在数据层存在但**界面不渲染** |
| 讲解完成自动朗读 | 朗读合理，但**不应替代文字展示** |

**数据流与 UI 脱节**：

```
explainSelection() → sendMessage() → readingPromptMessages / chatMessages 更新
                                    → readingAssistantPage 不显示 chatList
                                    → 仅 speakExplanation() 可感知结果
```

**建议（高优先级）**：
1. 读书助手 Tab 采用 **上下分栏** 或 **子 Tab**：「讲解对话」+「翻译」。
2. 至少恢复 `chatList` 展示，与 AI 进化 Tab 对称。
3. 流式生成时应在读书助手区显示 `streamingBubble`。

---

### 3.8 右页 — 翻译编辑区（lessonPlanEditor）

**组成**：
- 可编辑 `SelectableTextView`（editor 样式）
- 空态占位：「点击逐字翻译开始」
- 右上角：全选、朗读浮动按钮
- 生成中：右上角 ProgressView 遮罩

**合理性评估**：⭐⭐⭐⭐ **较合理**

| 优点 | 问题 |
|------|------|
| 翻译结果可编辑、可保存 | 与讲解区争用右页全部空间 |
| 空态引导清晰 | 逐字/整段翻译入口仅在顶栏菜单，与占位文案一致但路径长 |
| 朗读按钮就近放置 | 生成时遮罩 `allowsHitTesting(false)` 合理 |

---

### 3.9 右页 — AI 进化 Tab

**组成**：
1. **EvolutionUtilityTabsPanel**（进化 / 队列 子 Tab）
   - 进化：执行/停止/清空、后端与模型、Token 与控制条
   - 队列：左页编号命令列表、完成状态、源码路径
2. **EvolutionRebuildBanner**（构建安装时）
3. **chatList** — 对话 + Agent 执行轨迹
4. **Composer** — 进化追问

**合理性评估**：⭐⭐⭐⭐⭐ **整体优秀**

| 优点 | 问题 |
|------|------|
| Agent 轨迹卡片可展开、可复制、可定位文件 | 面板占用纵向空间较大，对话区相对压缩 |
| 进化/队列子 Tab 节省空间 | 与顶栏「进化」按钮、后端切换有功能重叠 |
| Token 预算与超限提示 | 对非技术用户略复杂，但有必要 |
| 流式思考 + 工具步骤展示完整 | — |

---

### 3.10 对话气泡（chatList）

**组成**：
- 用户消息：右对齐金色气泡
- 助手消息：左对齐白底气泡 + 可选 `AssistantExecutionTraceCard`
- 右键菜单：编辑/保存/朗读/复制/删除（助手）；复制/删除（用户）
- 流式态：`streamingBubble`

**合理性评估**：⭐⭐⭐⭐ **组件设计合理，但读书 Tab 未复用**

| 优点 | 问题 |
|------|------|
| 书籍风格与纸面协调 | 读书场景下组件闲置 |
| 执行轨迹仅进化 Tab 展示，避免干扰读书 | 讲解时 `showsExecutionTrace = false`，合理 |
| 消息可编辑、可朗读 | — |

---

### 3.11 输入区 — LLMComposerView / CursorComposerView

**共同结构**：紧凑控制条 + 多行输入框 + 发送/停止行；⌘↩ 发送。

**读书助手模式**：
- DisclosureGroup 展开：提供商、模型、上下文占比、用量条
- 摘要行：模型名 + 紧凑用量仪表

**AI 进化模式**：
- 摘要：「模型见上方进化 Tab」+ 用量仪表
- 避免与进化面板重复配置

**合理性评估**：⭐⭐⭐⭐ **较合理**

| 优点 | 问题 |
|------|------|
| 双 Composer 对称，按后端自动切换 | 读书 Tab 有 Composer 无对话区，体验断裂 |
| 进化模式避免重复模型选择 | Ollama 模型扫描 UI 略密 |
| 上下文滑块与用量可视化专业 | — |

---

### 3.12 配置提示 — LLMConfigPanel / CursorConfigPanel

**触发**：未配置对应后端时，在右页顶部展示内联配置卡片。

**合理性评估**：⭐⭐⭐⭐⭐ **非常合理**

| 优点 | 问题 |
|------|------|
| 就地配置，降低阻断 | — |
| Cursor 不可用时 LLM 回退 Banner 清晰 | — |
| 测试连接、显示/隐藏 Key | — |

---

### 3.13 设置面板（SettingsView）

**结构**：Sheet 模态；皮革顶栏 + Tab 切换（AI模型 / 语音）+ 纸面滚动内容。

**AI 模型设置区**：
- 多提供商配置（`LLMProfilesSettingsView`）
- Cursor Key / 模型 / Bridge 路径
- 自动升级 Toggle
- 说明文字

**语音设置区**：
- `ExplanationVoiceSettingsView`（引擎、语言处理、声音、下载）
- 关于语音说明

**合理性评估**：⭐⭐⭐⭐ **较合理**

| 优点 | 问题 |
|------|------|
| 与主界面风格统一 | `openSettings()` 强制切 AI 进化 Tab 再弹 Sheet，路径绕 |
| AI / 语音分 Tab 清晰 | 部分说明与右页内联配置重复 |
| 进化相关设置与 AI 放一起合理 | — |

---

### 3.14 语音设置子页（ExplanationVoiceSettingsView）

| 控件 | 评估 |
|------|------|
| 语音引擎 Segmented（4 档） | ⭐⭐⭐⭐⭐ 清晰 |
| 语言处理 Segmented（3 档） | ⭐⭐⭐⭐⭐ 与朗读质量直接相关 |
| 讲解声音 Picker（分组：系统/已下载） | ⭐⭐⭐⭐ 合理；需下载的模型有额外步骤 |
| 试听 / 下载按钮 | ⭐⭐⭐⭐⭐ 必要反馈 |
| 推荐在线女声列表 | ⭐⭐⭐⭐ 降低选择成本 |

---

## 4. 交互流程评估

### 4.1 核心阅读讲解流

```
左页选中 → 顶栏更多→讲解（或⌘R）
  → 切到读书助手 Tab
  → AI 生成（界面无流式/结果展示）  ← 问题点
  → 自动朗读
  → 可在底部输入追问（仍无可见历史）  ← 问题点
```

**评估**：⭐⭐ **流程断裂** — 听得到、看不到、追问不直观。

---

### 4.2 翻译流

```
左页有文 → 更多→逐字/整段翻译
  → 右页翻译区流式写入（全文替换或追加）
  → 可编辑、保存、朗读
```

**评估**：⭐⭐⭐⭐ **较完整** — 主区与功能匹配；入口可再简化。

---

### 4.3 名著补充流

```
左页粘贴节选 → 顶栏名著补充（⌘⇧C）
  → 左页展示流式预览或完成后替换
  → 自动保存
```

**评估**：⭐⭐⭐⭐ **合理** — 结果回写左页符合「阅读原文」心智；仅支持 LLM API 应在 UI 标明。

---

### 4.4 自我进化流

```
左页编号命令 → AI进化 Tab → 进化
  → 工具面板 + 对话轨迹
  → 完成写回左页「已完成」
  → build-and-install → 重启 → 自动下一条
```

**评估**：⭐⭐⭐⭐⭐ **闭环完整** — 产品差异化能力强；信息密度高适合 power user。

---

### 4.5 朗读流

```
讲解完成自动朗读 / 手动朗读 / 朗读翻译
  → ExplanationSpeechReader
  → 顶栏与浮动按钮显示「停止」
```

**评估**：⭐⭐⭐⭐ **合理** — 与讲解配合好；讲解无文字时朗读成为唯一输出，加重上文问题。

---

## 5. 组件库索引

| 组件 | 文件 | 用途 |
|------|------|------|
| `BookTheme` | BookTheme.swift | 色彩、字体、渐变 |
| `BookInterface` | BookInterface.swift | 书壳、丝带、页角、空态 |
| `BookActionButton` | BookTheme.swift | 顶栏深色按钮 |
| `BookPageActionButton` | BookTheme.swift | 纸面浅色按钮 |
| `BookStatusPill` | BookTheme.swift | 状态胶囊 |
| `BookToolbarMenuButton` | BookTheme.swift | 「更多」菜单触发器 |
| `SelectableTextView` | SelectableTextView.swift | 左页阅读 / 右页翻译编辑 |
| `ExplanationChatView` | ExplanationChatView.swift | 右页容器 |
| `LLMComposerView` | LLMComposerView.swift | 大模型输入区 |
| `CursorComposerView` | CursorComposerView.swift | Cursor 输入区 |
| `EvolutionUtilityTabsPanel` | EvolutionExecutionViews.swift | 进化工具面板 |
| `AssistantExecutionTraceCard` | EvolutionExecutionViews.swift | Agent 轨迹卡片 |
| `LLMConfigPanel` | LLMConfigPanel.swift | 内联大模型配置 |
| `CursorConfigPanel` | CursorConfigPanel.swift | 内联 Cursor 配置 |
| `SettingsView` | SettingsView.swift | 系统设置 Sheet |
| `ExplanationVoiceSettingsView` | ExplanationVoiceSettingsView.swift | 语音子设置 |

---

## 6. 合理性总评矩阵

| 功能模块 | 合理性 | 评分 | 主要结论 |
|----------|--------|------|----------|
| 书籍视觉风格 | 优秀 | 5/5 | 差异化强，与读书场景一致 |
| 左页阅读编辑 | 优秀 | 5/5 | 排版与 IME 处理到位 |
| 右页双 Tab 架构 | 良好 | 4/5 | 读书 vs 进化分离清晰 |
| **读书助手讲解展示** | **差** | **2/5** | **对话区缺失，严重 UX 缺陷** |
| 翻译编辑区 | 良好 | 4/5 | 功能完整，入口偏深 |
| AI 进化界面 | 优秀 | 5/5 | Agent 轨迹、队列、Token 齐全 |
| 顶栏信息架构 | 一般 | 3/5 | 胶囊多、后端切换跳转干扰 |
| 设置面板 | 良好 | 4/5 | 结构清晰，与内联配置有重叠 |
| 语音设置 | 优秀 | 5/5 | 引擎/语言/声音分层合理 |
| 内联配置提示 | 优秀 | 5/5 | 降低首次使用门槛 |
| 快捷键体系 | 良好 | 4/5 | 覆盖主操作；讲解应更突出 |
| 状态反馈 | 良好 | 4/5 | 未保存、进化队列、构建 Banner 到位 |
| 空态设计 | 良好 | 4/5 | 左页、翻译区空态明确 |

**综合 UI 成熟度**：⭐⭐⭐½（3.5/5）  
进化与设计语言领先；读书助手「讲解可见性」拖累整体体验。

---

## 7. 问题清单与改进优先级

### P0 — 必须修复

| # | 问题 | 建议方案 |
|---|------|----------|
| 1 | 读书助手 Tab 不显示讲解对话与流式输出 | 增加讲解对话区（分栏或子 Tab），复用 `chatList` |
| 2 | 有 Composer 无对话历史，追问体验断裂 | 与 #1 同步解决 |

### P1 — 强烈建议

| # | 问题 | 建议方案 |
|---|------|----------|
| 3 | 切换 AI 后端强制跳转 AI 进化 Tab | 移除自动跳转，或仅在进化上下文切换 |
| 4 | 「讲解」藏在「更多」菜单 | 提升为顶栏一级按钮 |
| 5 | `openSettings()` 先切 AI 进化 Tab | 直接弹出 Sheet，保持当前 Tab |
| 6 | 后端切换在顶栏与进化面板重复 | 顶栏只读展示，编辑集中在设置或进化面板 |

### P2 — 体验优化

| # | 问题 | 建议方案 |
|---|------|----------|
| 7 | 左页「原文 vs 命令笔记」认知模糊 | 页眉增加模式标签 |
| 8 | 顶栏胶囊过多 | 响应式折叠次要标签 |
| 9 | 翻译与讲解争用右页全高 | 读书助手内增加「讲解 | 翻译」分段控件 |
| 10 | 名著补充仅 LLM | 按钮旁标注或未配置时禁用提示 |
| 11 | 进化面板纵向占用过大 | 可折叠工具面板，默认仅显示操作行 |

### P3 — 长期

| # | 问题 | 建议方案 |
|---|------|----------|
| 12 | 无深色模式 | 评估是否与书籍隐喻冲突 |
| 13 | 仅支持 `.txt` | 扩展格式或导入说明 |
| 14 | 无首次启动引导 | 基于 welcome 消息的分步引导 |

---

## 8. 推荐目标布局（读书助手 Tab）

以下为建议的信息架构，不改变功能，仅修复可见性：

```
┌─ 读书助手 ─────────────────────────────────────┐
│ [配置提示条]                                    │
│ ┌─ 分段：讲解对话 | 翻译 ─────────────────────┐ │
│ │                                             │ │
│ │  （讲解）chatList + streamingBubble         │ │
│ │  或                                         │ │
│ │  （翻译）lessonPlanEditor                   │ │
│ │                                             │ │
│ └─────────────────────────────────────────────┘ │
│ [LLM/Cursor Composer]                           │
└─────────────────────────────────────────────────┘
```

---

## 9. 无障碍与平台一致性

| 项 | 现状 | 评估 |
|----|------|------|
| 键盘快捷键 | 菜单栏 + 顶栏 Help 提示 | ⭐⭐⭐⭐ |
| 中文 IME | `ReadingTextView` 专门优化 | ⭐⭐⭐⭐⭐ |
| 文本选择 | 气泡 `textSelection(.enabled)` | ⭐⭐⭐⭐ |
| VoiceOver | 未见专门标注 | ⭐⭐ 待加强 |
| 动态字体 | 固定宋体字号 | ⭐⭐ 未跟随系统 |

---

## 10. 与功能文档的 UI 映射

| 功能文档章节 | 主要 UI 承载 |
|--------------|--------------|
| 左页文档管理 | ContentView.leftPage |
| 读书讲解 | 顶栏更多→讲解；**应对应 ExplanationChatView 对话区（当前缺失）** |
| 文章翻译 | lessonPlanEditor + 顶栏更多 |
| 名著补充 | 顶栏 documentActionBar |
| 语音朗读 | 顶栏更多、翻译区按钮、消息右键菜单 |
| 自我进化 | AI进化 Tab 全套 |
| AI 双后端 | 顶栏切换 + EvolutionModelSettingsSection |
| 设置 | SettingsView Sheet |

---

*本文档基于 `ContentView`、`ExplanationChatView`、`BookTheme`、`SettingsView`、`EvolutionExecutionViews` 等源码归纳，并包含对当前实现与预期体验差距的评估。*
