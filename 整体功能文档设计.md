# AIBook 整体功能文档设计

> 文档版本：基于当前源码（Swift / macOS 13+）  
> 项目标识：`ai-book` · 应用名称：**AIBook** · 定位：**辅助读书**

---

## 1. 产品概述

### 1.1 定位

AIBook 是一款 macOS 原生辅助读书应用。用户左页阅读本地文本或编辑命令笔记，右页通过 AI 获取讲解、翻译与进化能力。整体 UI 采用「翻开的书本」视觉隐喻（皮革封面、纸张纹理、书脊丝带、页码装饰）。

### 1.2 核心价值

| 能力 | 说明 |
|------|------|
| 辅助阅读 | 选中左页文字，AI 讲解字面含义、上下文与难点 |
| 文章翻译 | 逐字翻译 / 整段翻译，结果可编辑、保存、朗读 |
| 名著补全 | 识别左页节选，由 AI 补全为连贯完整篇章 |
| 语音朗读 | 讲解与翻译内容支持系统语音与在线神经女声 |
| 自我进化 | 左页编号命令驱动 AI 改码、构建、重启，持续升级应用本身 |

### 1.3 技术栈

- **语言 / 框架**：Swift 5.9、SwiftUI、AVFoundation
- **平台**：macOS 13+
- **构建**：Swift Package Manager（`Package.swift`）
- **外部依赖**：Node.js 18+（`cursor-bridge`、`speech-bridge`）

---

## 2. 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        AIBookApp (SwiftUI)                       │
│  ContentView ── ReadingViewModel ── AppSettings (单例)          │
└────────────┬───────────────────────────────┬────────────────────┘
             │                               │
    ┌────────▼────────┐              ┌───────▼────────┐
    │   左页（原文）   │              │   右页（AI）    │
    │  SelectableText │              │  读书助手 Tab   │
    │  ReadmeNotes    │              │  AI 进化 Tab    │
    └────────┬────────┘              └───────┬────────┘
             │                               │
             │         ┌─────────────────────┼─────────────────────┐
             │         │                     │                     │
             │    LLMService          CursorService         ExplanationSpeechReader
             │  (OpenAI 兼容 API)    (cursor-bridge)       (系统 TTS + Edge TTS)
             │         │                     │                     │
             │    LLMEvolutionAgent    explain.mjs           speech-bridge
             │  (工具调用改码)         (Agent 流式)         synthesize.mjs
             └─────────────────────────────────────────────────────────
```

### 2.1 分层说明

| 层级 | 职责 |
|------|------|
| **表现层** | `ContentView`、`ExplanationChatView`、`LLMComposerView`、`CursorComposerView`、`SettingsView` |
| **视图模型** | `ReadingViewModel`：文件、选中、对话、翻译、进化、朗读状态机 |
| **领域服务** | `ReadingAssistant`、`TranslationAssistant`、`EvolutionAssistant`、`ClassicLiteratureSupplement` |
| **AI 连接器** | `LLMService`、`CursorService`、`LLMEvolutionAgent` |
| **基础设施** | `AppSettings`、`ChatSessionStore`、`ReadmeNotesStore`、`SpeechVoiceStore` |
| **外部桥接** | `cursor-bridge`（Cursor Agent）、`speech-bridge`（Microsoft Edge TTS） |

---

## 3. 用户界面设计

### 3.1 主窗口布局

```
┌──────────────────────────────────────────────────────────────────┐
│  顶栏：品牌 · 文件名 · 模式胶囊 · 大模型/Cursor 切换              │
│  工具栏：打开/保存 | 读书助手/更多 | AI进化/设置/进化              │
├────────────────────────┬─────────────────────────────────────────┤
│                        │                                         │
│   — 左页 —             │   — 右页 —                              │
│   可编辑文本区          │   Tab：读书助手 | AI进化                 │
│   （原文 / 命令笔记）   │   对话流 + 输入框 + 配置面板             │
│                        │   （翻译区在读书助手内）                  │
│                        │                                         │
└────────────────────────┴─────────────────────────────────────────┘
```

- 默认窗口：1280 × 860，最小 960 × 640
- 强制浅色主题（`.preferredColorScheme(.light)`）
- 隐藏标题栏，统一工具栏样式

### 3.2 右页 Tab

| Tab | 标识 | 功能 |
|-----|------|------|
| **读书助手** | `RightPageTab.readingAssistant` | 讲解对话、翻译编辑、朗读 |
| **AI进化** | `RightPageTab.aiEvolution` | 按左页编号命令升级源码，展示 Agent 执行轨迹 |

两套 Tab **独立维护**对话历史（`readingMessages` / `evolutionMessages`）与输入框状态。

### 3.3 顶栏状态胶囊

- 当前文件名 / 未保存提示
- 当前 AI 后端：`大模型 API` 或 `Cursor 本地`
- 大模型模式下显示 `提供商 · 模型`
- 进化队列状态（待办条数、下一条编号）

### 3.4 设置面板

分两个子 Tab：

| Tab | 内容 |
|-----|------|
| **AI模型设置** | 大模型提供商、API Key、模型、上下文占比；Cursor API Key、模型、桥接路径；自动升级开关 |
| **语音设置** | 语音引擎、语言处理、讲解声音选择与下载 |

---

## 4. 核心功能模块

### 4.1 左页文档管理

#### 4.1.1 打开与编辑

- 支持打开本地 `.txt` 文件（⌘O、拖拽、工具栏）
- 左页全文可编辑，支持选中文字
- 未打开文件时，默认加载**命令笔记**（见 4.1.2）

#### 4.1.2 命令笔记（Readme Notes）

- 启动时从 `~/Library/Application Support/AIBook/readme-notes.txt` 加载
- 若无本地副本，依次回退：Bundle 内 `readme.txt` → 项目根 `readme.txt`
- ⌘S 保存到 Application Support
- 用于编写**编号进化命令**（如 `1、实现某某功能`）

#### 4.1.3 保存与导出

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 保存 | ⌘S | 保存左页到当前文件或命令笔记 |
| 另存为 | ⌘⇧S | 左页全文另存为新 `.txt` |
| 导出选中 | ⌘⌥S | 将左页选中内容导出 |

### 4.2 读书讲解

#### 4.2.1 流程

1. 用户在左页选中文字（或依赖全文上下文）
2. 点击「讲解」或 ⌘R
3. 系统将选中内容 + 原文节选 + 历史对话组装为 Prompt
4. 调用当前 AI 后端（大模型 API 或 Cursor 本地）流式返回
5. 回复完成后，**自动朗读**讲解内容（可停止）

#### 4.2.2 对话追问

- 读书助手 Tab 底部输入框支持多轮追问
- 重启应用后通过 `ChatSessionStore` 恢复对话上下文

#### 4.2.3 Persona

`ReadingAssistant.systemPrompt` 定义读书助手角色：清晰中文、解释字面含义与上下文、简洁有条理。

### 4.3 文章翻译

翻译功能集成在**读书助手**右页，与讲解共用 AI 后端。

#### 4.3.1 翻译模式

| 模式 | 入口 | 说明 |
|------|------|------|
| **逐字翻译** | 顶栏「更多」→ 逐字翻译 | 按原文顺序逐字/逐词拆解，附极简说明与一句话总译 |
| **整段翻译** | 顶栏「更多」→ 整段翻译 | 保留段落层次，通顺准确；可参考已有翻译改写 |

#### 4.3.2 翻译区操作

- 翻译结果在 `lessonPlanContent` 中展示，**可编辑**
- 支持：打开翻译文件、保存翻译（`{文件名}-翻译.txt`）、清空、全选、朗读翻译（⌘⌥T）

#### 4.3.3 翻译源文本

优先使用左页**选中文字**；若无选中则使用左页全文。

### 4.4 名著补充

| 项目 | 内容 |
|------|------|
| 入口 | 顶栏「名著补充」、⌘⇧C |
| 能力 | 识别左页节选所属名著，补全为完整连贯篇章 |
| 输出格式 | `<<<名著补充>>>…<<<END>>>` 标记块 |
| 行为 | 补全内容写回左页（替换选中或全文），并自动保存 |

`ClassicLiteratureSupplement` 要求保持原著风格，不现代口语化改写。

### 4.5 语音朗读

#### 4.5.1 朗读对象

- 讲解选中 / 全文（⌘⌥R）
- 翻译内容（⌘⌥T）
- 单条 AI 回复（右键菜单）
- 设置页「试听当前声音」

#### 4.5.2 语音引擎模式

| 模式 | 说明 |
|------|------|
| 极速（本地） | 系统 `AVSpeechSynthesizer`，首句延迟最低 |
| 平衡（在线） | Edge TTS 24kHz / 48kbps |
| 高音质（在线） | Edge TTS 24kHz / 96kbps |
| 美声（在线） | 高码率 + 慢速咬字 + 动态韵律（默认） |

#### 4.5.3 语言处理模式

| 模式 | 说明 |
|------|------|
| 高效 | 更少改写，切句更长 |
| 平衡 | 速度与清晰度均衡 |
| 深度 | 更短分句、标点停顿、缩写拆读、多音字消歧（默认） |

#### 4.5.4 讲解声音

- **系统中文语音**：macOS 内置，按质量优选女声
- **在线神经女声**（Edge TTS）：晓伊（默认）、晓晓、方言/粤语/台湾女声等
- 在线女声需先下载样本（`SpeechVoiceDownloader`）到 `~/Library/Application Support/AIBook/voices/{voiceID}/`

#### 4.5.5 文本预处理管线（`SpeechTextSanitizer`）

```
Markdown 原文
  → 剥离代码块/链接/标题符号
  → 韵律增强（深度/平衡模式）
  → 按标点切分为 SpeechUnit
  → spokenForm：保留中文标点、长句呼吸切分、多音字轻分隔
  → TTS 合成（逐句 + 句间停顿）
```

### 4.6 自我进化

AIBook 的独特能力：通过左页**编号命令**驱动 AI 修改自身源码。

#### 4.6.1 命令格式

左页每行一条编号命令，例如：

```
1、已完成：实现左右分栏阅读
2、增加语音朗读功能
3、优化汉字咬字清晰度
```

- 解析器：`EvolutionPlanner.parseCommands`
- 完成标记：行内含 `已完成` / `已修复` 等

#### 4.6.2 进化流程

```
用户点击「进化」(⌘E) 或自动升级触发
  → 取下一条待办命令
  → 构建进化 Prompt（产品定义 + 全部命令 + 源码路径）
  → AI 执行（Cursor Agent 或 LLM 工具调用）
  → 流式展示思考过程 + 工具步骤
  → 从回复提取「N、已完成：…」写回左页
  → 执行 scripts/build-and-install.sh
  → 重启应用
  → 若开启自动升级且仍有待办 → 继续下一条
```

#### 4.6.3 双通道进化

| 通道 | 实现 | 特点 |
|------|------|------|
| **Cursor 本地** | `cursor-bridge/explain.mjs` | 完整 Agent 工具链，流式思考 + 工具步骤 |
| **大模型 API** | `LLMEvolutionAgent` | OpenAI 兼容工具调用，沙箱内 `EvolutionLocalTools` |

进化优先 Cursor；不可用时回退大模型 API。

#### 4.6.4 本地工具（LLM 进化）

`EvolutionLocalTools` 在 ai-book 源码目录内沙箱执行：

- `read` / `grep` / `glob`：读搜文件
- `edit` / `write`：改码
- `shell`：运行命令（安全策略过滤）

#### 4.6.5 自动升级链

- 设置项：`autoEvolutionEnabled`（默认开启）
- 状态文件：`~/Library/Application Support/AIBook/auto-evolution-active`
- 应用启动 2.5s 后若有待办命令 → 自动切到 AI 进化 Tab 并开始执行

#### 4.6.6 Token 预算

进化与追问受 `ModelTokenBudget` 约束，超限阻止发送；界面显示已用/剩余 Token。

---

## 5. AI 集成设计

### 5.1 双后端切换

顶栏右侧可在两种后端间切换（进化与讲解共用）：

| 后端 | 枚举 | 连接方式 |
|------|------|----------|
| 大模型 API | `ExplanationSource.llm` | OpenAI 兼容 HTTP API |
| Cursor 本地 | `ExplanationSource.cursor` | Node.js 子进程调用 `cursor-bridge` |

切换前由 `AppGuard` 校验配置完整性。

### 5.2 大模型提供商

`LLMProvider` 内置：

| 提供商 | 默认 Base URL | 默认模型 |
|--------|---------------|----------|
| OpenAI | `https://api.openai.com/v1` | gpt-4o-mini |
| DeepSeek | `https://api.deepseek.com/v1` | deepseek-chat |
| Moonshot | `https://api.moonshot.cn/v1` | moonshot-v1-8k |
| 通义千问 | DashScope 兼容地址 | 见 `QwenBailianConfig` |
| 智谱 | `https://open.bigmodel.cn/api/paas/v4` | glm-4-flash |
| Ollama | `http://127.0.0.1:11434/v1` | llama3.2 |
| 自定义 | 用户填写 | 用户填写 |

- 各提供商配置独立存储于 `LLMProfileStore`（按提供商分 Key）
- Ollama 可不填 API Key；支持 `OLLAMA_HOST` / `OLLAMA_API_KEY` 环境变量
- 设置页可扫描本机 Ollama 已 pull 模型列表

### 5.3 Cursor 本地配置

| 配置项 | 说明 |
|--------|------|
| `cursorAPIKey` | Cursor API Key（或环境变量 `CURSOR_API_KEY`） |
| `cursorModel` | 如 `composer-2.5`、`composer-2`、`auto`、`gpt-4o`、`claude-sonnet-4` |
| `cursorBridgePath` | `cursor-bridge` 目录路径 |
| `cursorContextPercent` | 左页原文节选占比（5%–100%） |

### 5.4 上下文组装

- **大模型**：`llmContextPercent` 控制原文节选比例
- **Cursor**：`CursorContextCalculator` 估算 Prompt 字符数，支持以选中文字为锚点居中节选
- 历史对话过滤欢迎消息，避免污染上下文

### 5.5 流式输出

| 事件 | 大模型 | Cursor |
|------|--------|--------|
| 思考增量 | `reasoning` 流 | `thinkingDelta` |
| 文本增量 | `content` 流 | `textDelta` |
| 工具步骤 | LLM 工具调用结果 | `toolUpdate`（名称/状态/详情） |
| 完成 | `done` | `done(text, thinking)` |

AI 进化 Tab 将思考与工具步骤渲染为可展开的 **Agent 执行过程**时间线。

---

## 6. 数据持久化

### 6.1 存储位置

根目录：`~/Library/Application Support/AIBook/`

| 文件 / 目录 | 用途 |
|-------------|------|
| `readme-notes.txt` | 左页命令笔记 |
| `chat-session.json` | 对话会话（读书 + 进化双通道） |
| `auto-evolution-active` | 自动升级链标志 |
| `voices/{voiceID}/` | 在线女声样本与 manifest |

### 6.2 UserDefaults 键

| 键前缀 | 内容 |
|--------|------|
| `aiBook.provider` / `baseURL` / `model` / `apiKey` | 当前大模型配置 |
| `aiBook.explanationSource` | AI 后端选择 |
| `aiBook.cursor*` | Cursor 相关配置 |
| `aiBook.autoEvolutionEnabled` | 自动升级开关 |
| `aiBook.speechEngineMode` | 语音引擎模式 |
| `aiBook.speechLanguageMode` | 语言处理模式 |
| `aiBook.explanationVoiceID` | 当前讲解声音 |
| `aiBook.downloadedNeuralVoiceIDs` | 已下载在线女声 |

大模型各提供商详细配置由 `LLMProfileStore` 管理（独立 JSON）。

### 6.3 会话恢复

`ChatSessionStore` 持久化：

- `readingMessages` / `evolutionMessages`
- `readingChatInput` / `evolutionChatInput`
- `rightPageTab`
- `lastOpenedFilePath`
- `lessonPlanContent`（翻译内容）

应用 `onAppear` 时自动加载。

---

## 7. 外部桥接

### 7.1 cursor-bridge

```
cursor-bridge/
├── package.json
└── explain.mjs      # Cursor Agent 流式调用
```

- Swift 通过 `Process` 启动 Node.js 子进程
- stdin 传入 JSON（prompt、model、apiKey、workingDirectory）
- stdout 按行输出 JSON 事件（thinking、text、tool、done、error）
- 依赖：`cursor-bridge` 目录下 `npm install`

### 7.2 speech-bridge

```
speech-bridge/
├── package.json
└── synthesize.mjs   # Microsoft Edge TTS 合成
```

- 输入：text、voice、outputFormat、rate、pitch、volume
- 输出：MP3 音频（文件或 Base64）
- 依赖：`msedge-tts` npm 包

### 7.3 桥接目录发现

按优先级查找：

1. App Bundle `Resources/` 内嵌桥接
2. 项目根目录（`CursorService.defaultProjectDirectory()`）
3. 用户配置的自定义路径

---

## 8. 快捷键总表

| 快捷键 | 功能 |
|--------|------|
| ⌘O | 打开文本文件 |
| ⌘S | 保存左页 |
| ⌘⇧S | 另存为 |
| ⌘⌥S | 导出选中 |
| ⌘R | 讲解选中 |
| ⌘⌥R | 朗读（选中/全文） |
| ⌘⌥T | 朗读翻译 |
| ⌘⇧C | 名著补充 |
| ⌘E | 自我进化（下一条命令） |
| ⌘⌃A | 全选左页 |
| ⌘⌃⇧A | 全选翻译 |

---

## 9. 模块职责索引

| 模块 | 文件 | 职责 |
|------|------|------|
| 应用入口 | `AIBookApp.swift` | 窗口、菜单命令 |
| 主界面 | `ContentView.swift` | 顶栏、左右分栏、工具栏 |
| 视图模型 | `ReadingViewModel.swift` | 全部业务状态与编排 |
| 全局设置 | `AppSettings.swift` | 配置读写、提供商切换 |
| 大模型服务 | `LLMService.swift` | OpenAI 兼容 API 调用 |
| Cursor 服务 | `CursorService.swift` | 桥接进程管理、流解析 |
| LLM 进化代理 | `LLMEvolutionAgent.swift` | 工具调用循环 |
| 本地进化工具 | `EvolutionLocalTools.swift` | 沙箱文件/命令操作 |
| 进化规划 | `EvolutionPlanner.swift` | 命令解析与 Prompt 构建 |
| 自动进化协调 | `AutoEvolutionCoordinator.swift` | 跨重启升级链 |
| 应用重启 | `AppRelauncher.swift` | build-and-install + relaunch |
| 读书助手 | `ReadingAssistant.swift` | 讲解 Persona |
| 翻译助手 | `TranslationAssistant.swift` | 翻译 Persona |
| 进化助手 | `EvolutionAssistant.swift` | 进化 Persona |
| 名著补充 | `ClassicLiteratureSupplement.swift` | 补全 Prompt 与解析 |
| 语音朗读 | `ExplanationSpeechReader.swift` | TTS 播放编排 |
| 文本清洗 | `SpeechTextSanitizer.swift` | Markdown→朗读文本 |
| 语音目录 | `SpeechVoiceCatalog.swift` | 系统/在线声音列表 |
| Edge TTS | `EdgeTTSService.swift` | 在线合成封装 |
| 语音桥接 | `SpeechBridgeService.swift` | Node 子进程调用 |
| 会话存储 | `ChatSessionStore.swift` | 对话持久化 |
| 笔记存储 | `ReadmeNotesStore.swift` | 命令笔记 |
| 配置守卫 | `AppGuard.swift` | 配置校验、未保存确认 |
| 书籍 UI | `BookTheme.swift` / `BookInterface.swift` | 视觉主题与装饰 |
| 产品定义 | `AIBookProduct.swift` | 品牌与定位文案 |

---

## 10. 部署与运行

### 10.1 开发运行

```bash
cd ai-book
swift run
```

### 10.2 安装到应用程序

```bash
./scripts/build-and-install.sh
```

安装目标：`/Applications/AIBook.app`（含图标、桥接模块拷贝）。

### 10.3 环境准备清单

| 依赖 | 用途 | 安装 |
|------|------|------|
| Xcode / Swift 5.9+ | 编译运行 | 系统 |
| Node.js 18+ | 桥接脚本 | 自行安装 |
| cursor-bridge | Cursor 本地模式 | `cd cursor-bridge && npm install` |
| speech-bridge | 在线女声 | `cd speech-bridge && npm install` |
| Ollama（可选） | 本地大模型 | `ollama serve` |

### 10.4 推荐默认配置

| 设置项 | 推荐值 |
|--------|--------|
| 语音引擎 | 美声（在线） |
| 语言处理 | 深度 |
| 讲解声音 | 晓伊（zh-CN-XiaoyiNeural） |
| 自动升级 | 开启 |

---

## 11. 用户典型工作流

### 11.1 日常阅读

```
打开 .txt → 选中段落 → 讲解 → 自动朗读 → 追问细节
```

### 11.2 外文/古文翻译

```
打开文章 → 逐字翻译 → 编辑右页翻译 → 整段润色 → 保存/朗读
```

### 11.3 名著研读

```
粘贴节选 → 名著补充 → 获得完整篇章 → 分段讲解
```

### 11.4 功能自升级

```
左页写编号命令 → 进化(⌘E) → 观察 Agent 轨迹 → 自动构建重启 → 下一条
```

---

## 12. 扩展与约束

### 12.1 设计约束

- 进化改码限制在 ai-book 源码目录内（`EvolutionLocalTools` 路径沙箱）
- Shell 命令受安全策略过滤
- Token 上限按模型动态计算（`ModelTokenBudget`）
- 单实例进化任务，运行中可停止

### 12.2 已知边界

- 仅支持 `.txt` 纯文本左页
- 在线女声依赖 Microsoft Edge TTS 服务可用性
- Cursor 本地模式需有效 API Key 与桥接依赖
- Ollama 进化若模型不支持工具调用，仅能文字方案无法自动改文件

---

## 附录 A：项目目录结构

```
ai-book/
├── Package.swift
├── 整体功能文档设计.md          # 本文档
├── scripts/
│   ├── build-and-install.sh    # 发布构建与安装
│   ├── generate-icon.sh
│   └── setup-cursor-key.sh
├── cursor-bridge/                # Cursor Agent 桥接
├── speech-bridge/                # Edge TTS 桥接
├── App/Info.plist
└── Sources/AIBook/               # Swift 源码（53 个模块）
    ├── AIBookApp.swift
    ├── ContentView.swift
    ├── ReadingViewModel.swift
    ├── AppSettings.swift
    ├── LLMService.swift
    ├── CursorService.swift
    ├── ExplanationSpeechReader.swift
    ├── SpeechTextSanitizer.swift
    ├── SelfEvolution.swift
    ├── EvolutionPlanner.swift
    └── Resources/
        └── sample.txt            # 示例文本
```

---

## 附录 B：核心数据流（讲解）

```
用户选中文字
  → ReadingViewModel.explainSelection()
  → buildChatPrompt(fileExcerpt, selection, input)
  → AppSettings.explanationSource 路由
      ├─ .llm  → LLMService.streamChat()
      └─ .cursor → CursorService.streamExplain()
  → 流式更新 chatMessages / streamingResponse
  → 完成后 ExplanationSpeechReader.speak()
      ├─ SpeechTextSanitizer.speechUnits()
      ├─ 神经声 → EdgeTTSService → SpeechBridgeService
      └─ 系统声 → AVSpeechSynthesizer
```

---

*文档根据 `Sources/AIBook` 源码归纳，随功能演进需同步更新。*
