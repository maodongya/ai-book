# AIBook 原文–翻译同步滑动与翻页设计

> 文档性质：实现设计  
> 关联：[aibook阅读模式.md](./aibook阅读模式.md) · [整体功能文档设计.md](./整体功能文档设计.md) · `ContentView` · `ExplanationChatView` · `SelectableTextView` · `BookPaginator`

---

## 1. 背景与目标

### 1.1 现状

学习模式下，左页原文与右页翻译是**两个独立的 `SelectableTextView`**：

| 区域 | 数据 | 滚动 |
|------|------|------|
| 左页 | `fileContent` | 独立 `NSScrollView` |
| 右页翻译 Tab | `lessonPlanContent` | 独立 `NSScrollView` |

翻译由 LLM 生成后写入 `lessonPlanContent` 字符串（见 `ReadingViewModel.generateLessonPlan` / `refineLessonPlan`）。**没有**原文区间（`NSRange`）与译文块之间的结构化关联，因此：

- 左页滚动时，右页不会跟随；
- 右页滚动时，无法定位对应原文；
- 阅读模式的翻页只分页 `fileContent`，与翻译无关。

### 1.2 目标

1. **整段翻译**：按段落建立原文 ↔ 译文块映射，学习模式下**同步滚动**。
2. **逐字翻译**：按字/词/短语行建立映射，学习模式下**同步滚动**，并支持块级高亮。
3. **阅读模式翻页（扩展）**：在已有原文 spread 翻页基础上，支持**译文对照翻页**——左页原文第 N 页、右页对应译文第 N 页，翻页动画与 spread 索引联动。
4. **持久化**：对齐关系随会话保存；原文大改后给出「对齐失效」提示。

### 1.3 非目标（首版不做）

- 用户手动拖拽调整对齐块边界
- 多语言 / 多译本并排
- 讲解对话（`chatMessages`）与原文的 scroll 同步
- 实时协同编辑冲突合并（CRDT）

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| **先对齐、再同步** | 同步滚动/翻页只读 `TranslationAlignment`，不依赖纯文本比例 |
| **块锚点，而非 scroll 比例** | 左页可见区域 → 命中 source 块 → 右页滚到对应 translation 块 |
| **双模式共用底座、分支解析** | 整段 / 逐字使用同一 `TranslationBlock` 模型，生成与解析管道不同 |
| **防反馈环** | `TranslationScrollSync.isPropagating` 避免左右 scroll 互相触发死循环 |
| **失效可感知** | 原文 hash 变化、用户大段删改译文时，对齐标记 stale 并提示重译 |

---

## 3. 对齐数据模型

### 3.1 核心类型

```swift
enum TranslationBlockLevel: String, Codable {
    case paragraph   // 整段翻译
    case word        // 逐字/逐词行
    case phrase      // 短语拆解行
    case summary     // 一句话总译、难点汇总等
}

struct TranslationBlock: Identifiable, Codable, Equatable {
    let id: UUID
    var sourceRange: NSRange          // 在 fileContent UTF-16 坐标
    var sourceText: String            // 冗余快照，便于校验
    var translationText: String
    var level: TranslationBlockLevel
    var order: Int                    // 全局排序
}

enum TranslationAlignmentMode: String, Codable {
    case paragraph
    case wordByWord
}

struct TranslationAlignment: Codable, Equatable {
    var mode: TranslationAlignmentMode
    var blocks: [TranslationBlock]
    var sourceContentHash: String     // fileContent 稳定 hash
    var createdAt: Date
    var isStale: Bool                 // 原文或译文被手动改乱
}
```

### 3.2 与现有字段关系

| 现有 | 新 | 关系 |
|------|-----|------|
| `lessonPlanContent` | `translationAlignment` | 展示文本由 blocks 拼接生成；可保留 string 作兼容与导出 |
| `lessonPlanSelectedText` | 不变 | 选中译文块时可反查 sourceRange 高亮左页 |
| `ChatSessionStore.lessonPlanContent` | `translationAlignment` JSON | 会话持久化扩展 |

### 3.3 块拼接规则（`lessonPlanContent` 兼容）

```
blocks.sorted(by: order)
  .map { format($0) }   // 按 mode 决定分隔符与标题
  .joined(separator: "\n\n")
```

整段模式：`format` 为译文段落 + 可选注释。  
逐字模式：`format` 为 `原文 → 译文` 行或表格行。

---

## 4. 方案一：整段翻译对齐与同步滚动

### 4.1 生成阶段

**LLM 输出改为结构化 JSON**（内部协议，UI 仍显示可读文本）：

```json
{
  "mode": "paragraph",
  "blocks": [
    {
      "sourceText": "第一段原文…",
      "translationText": "第一段译文…",
      "notes": "可选注释"
    }
  ]
}
```

**解析与锚定算法：**

1. 将 `fileContent` 按 `\n{2,}` 切分为段落数组 `paragraphs[]`。
2. 对每个 LLM block，在 `paragraphs` 中做**顺序模糊匹配**（trim、全角空格归一）。
3. 匹配成功 → 计算该段落在 `fileContent` 中的 `NSRange`，写入 `TranslationBlock(level: .paragraph)`。
4. 匹配失败 → 块仍保存，`sourceRange` 为 `{0,0}`，`isStale = true`，UI 显示「未锚定」。

**Prompt 调整**（`buildLessonPlanPrompt` · `.paragraph`）：

- 要求「段落数与顺序必须与原文一致」；
- 要求输出 JSON（可放在 markdown code fence 内，解析器提取）。

### 4.2 同步滚动（学习模式）

```
左页 scroll
  → visibleRange = leftTextView.visibleCharacterRange()
  → anchorBlock = blocks.last { intersects($0.sourceRange, visibleRange) }
                 ?? blocks.first { $0.sourceRange.location >= visibleRange.location }
  → rightTextView.scrollTo(show: anchorBlock.translationRange, anchor: .top)
```

- 右 → 左同理（可选，默认开启双向）。
- 顶栏翻译区增加 **「同步滚动」** Toggle（默认开），绑定 `TranslationScrollSync.enabled`。

### 4.3 高亮（可选增强）

- 左页滚动时：对 `anchorBlock.sourceRange` 施加临时 background attribute（`BookTheme.selection` 淡化）。
- 右页 hover/选中某段：左页跳到并高亮对应 `sourceRange`。

---

## 5. 方案二：逐字翻译对齐与同步滚动

### 5.1 生成阶段

**LLM 输出 JSON：**

```json
{
  "mode": "wordByWord",
  "entries": [
    { "source": "之", "translation": "的/它", "note": "" },
    { "source": "乎", "translation": "吗", "note": "语气词" }
  ],
  "summary": "一句话总译…",
  "hardPoints": ["难点1…"]
}
```

**锚定算法：**

1. 维护 `searchStart` 指针，按 `entries` 顺序在 `fileContent` 中查找 `source`（支持子串、跳过空白）。
2. 找到 → `sourceRange` = 匹配区间，`level = .word` 或 `.phrase`（长度 ≥ 4 字归为 phrase）。
3. 找不到 → 块保留，`sourceRange` 空，计入 stale。
4. `summary` / `hardPoints` → `level: .summary`，`sourceRange` 覆盖全文或 `{0, length}`。

**兼容旧格式：** 若 LLM 仍返回「原文 → 译文」纯文本，用行解析器 `TranslationLineParser` 回退解析，再跑同一锚定算法。

### 5.2 同步滚动

与整段共用 `TranslationScrollSync`，但：

- 块数量多，取 visibleRange **中心点** 所在块，避免顶部空白误命中；
- 右页 scroll 时按**块顶**对齐，不用居中（表格行高一致）。

### 5.3 对照视图（逐字专用 UI，Phase 2）

在翻译 Tab 增加子模式 **「表格对照」**：

| 原文 | 译文 | 说明 |
|------|------|------|
| … | … | … |

- 单行双列，`LazyVStack` + 共用外层 `ScrollView`，**天然同步滚动**（无需 NSScrollView 双端耦合）。
- 与学习模式现有「自由编辑区」并存，Toggle 切换。

---

## 6. 同步滚动架构（学习模式）

### 6.1 组件

```
Sources/AIBook/Reading/Translation/
├── TranslationBlock.swift           // 模型
├── TranslationAlignmentBuilder.swift  // 解析 LLM JSON + 锚定
├── TranslationLineParser.swift      // 纯文本回退解析
├── TranslationScrollSync.swift      // 同步控制器
└── TranslationAlignedTextView.swift // 可选：带 block 高亮的 TextView 封装
```

### 6.2 SelectableTextView 扩展

在现有 `NSScrollView` + `NSTextView` 封装上增加：

| API | 用途 |
|-----|------|
| `onVisibleRangeChange: (NSRange, CGFloat) -> Void` | 监听滚动，上报可见字符区间 |
| `scrollToCharacterRange(_ range: NSRange, anchor: VerticalAlignment)` | 滚到指定块 |
| `highlightRange(_ range: NSRange?, color: NSColor?)` | 临时高亮 |
| `characterRect(for range: NSRange) -> CGRect?` | 布局测量（可选） |

实现要点：

- 在 `Coordinator` 中监听 `NSView.boundsDidChangeNotification`（`contentView`）。
- IME 组合中（`hasMarkedText()`）暂停同步，避免抖动。

### 6.3 TranslationScrollSync

```swift
@MainActor
final class TranslationScrollSync: ObservableObject {
    @Published var isEnabled = true
    private var isPropagating = false
    weak var sourceView: SelectableTextViewProxy?
    weak var targetView: SelectableTextViewProxy?

    func sourceDidScroll(visibleRange: NSRange, alignment: TranslationAlignment) { … }
    func targetDidScroll(visibleRange: NSRange, alignment: TranslationAlignment) { … }
}
```

`ReadingViewModel` 持有 `translationAlignment` 与 `scrollSync`；`ContentView` 左页、`ExplanationChatView` 翻译区注册 proxy。

### 6.4 交互流程

```
┌─────────────┐     visibleRange      ┌──────────────────────┐
│ 左页原文     │ ───────────────────► │ TranslationScrollSync │
│ Selectable  │                      │  anchorBlock lookup    │
└─────────────┘                      └──────────┬───────────┘
                                                │ scrollToRange
┌─────────────┐                                 ▼
│ 右页翻译     │ ◄───────────────────  translation block
│ Selectable  │
└─────────────┘
```

---

## 7. 同步翻页（阅读模式扩展）

### 7.1 场景

用户已生成翻译对齐，进入阅读模式时可选 **「对照阅读」**：

- 左 spread：原文第 N、N+1 页（现有 `BookPaginator`）；
- 右 spread：**译文**第 N、N+1 页（对 `translationPlainText` 独立分页，页边界按 block 尽量不截断）。

### 7.2 分页策略

1. 从 `TranslationAlignment.blocks` 生成 `translationPlainText`（仅 `translationText` 拼接）。
2. `BookPaginator.paginate(translationPlainText)` → `translationPages[]`。
3. 建立 **页 → 块** 索引：`translationPageRanges[i]` 对应哪些 block id。
4. 建立 **原文页 → 译文页** 映射：
   - 对每个原文页字符区间，找覆盖该区间 sourceRange 的 blocks；
   - 这些 blocks 落在哪些 translationPages → 映射 `sourcePageIndex → translationPageIndex`（允许一对多，取首个）。

### 7.3 翻页联动

| 操作 | 行为 |
|------|------|
| 阅读模式 forward | `readingSpreadIndex += 1`；译文 spread 同步到映射后的 index |
| 仅原文 spread | 右页显示映射译文页；无映射则右页空白提示「本页暂无译文」 |
| 回到学习模式 | 恢复 scroll 位置到当前 spread 对应 sourceRange 顶部 |

### 7.4 UI

- 进入阅读模式前：若存在有效 `TranslationAlignment`，顶栏增加 **「对照翻页」** 开关。
- 对照开启：右页从「隐藏 AI」变为「译文纸页」（样式同 `pageRight`，无编辑）。
- 关闭：保持现有纯原文阅读。

---

## 8. 持久化与失效

### 8.1 ChatSessionStore 扩展

```swift
struct PersistedChatSession: Codable {
    // 现有字段…
    var lessonPlanContent: String?
    var translationAlignment: TranslationAlignment?  // 新增
    var scrollSyncEnabled: Bool?                   // 新增，默认 true
}
```

### 8.2 失效规则

| 事件 | 行为 |
|------|------|
| `fileContent` hash 变化 | `alignment.isStale = true`，同步滚动暂停 |
| 用户编辑 `lessonPlanContent` 且与 blocks 拼接不一致 | stale，提示「重新生成翻译以恢复对齐」 |
| 重新逐字/整段翻译成功 | 重建 alignment，清除 stale |

---

## 9. UI 入口

| 位置 | 控件 | 说明 |
|------|------|------|
| 翻译 Tab 顶栏 | 「同步滚动」Toggle | 控制 `TranslationScrollSync.isEnabled` |
| 翻译 Tab 顶栏 | 「表格对照」Toggle | 仅逐字模式；切换双列 ScrollView |
| 阅读模式顶栏 | 「对照翻页」Toggle | 有 alignment 时可用 |
| stale 状态 | 横幅提示 | 「原文已修改，对齐失效，请重新翻译」 |

---

## 10. 实现分期

### Phase 1 — 底座（整段 + 逐字共用）

- [ ] `TranslationBlock` / `TranslationAlignment` 模型
- [ ] `TranslationAlignmentBuilder` + JSON 解析
- [ ] `TranslationLineParser` 纯文本回退
- [ ] 修改 `buildLessonPlanPrompt` 输出 JSON
- [ ] `ReadingViewModel` 集成；`lessonPlanContent` 由 blocks 生成
- [ ] `ChatSessionStore` 持久化 alignment

### Phase 2 — 学习模式同步滚动

- [ ] `SelectableTextView` scroll API
- [ ] `TranslationScrollSync`
- [ ] 左页 + 翻译 Tab 接线
- [ ] 「同步滚动」Toggle + stale 提示
- [ ] 整段模式端到端验证
- [ ] 逐字模式端到端验证

### Phase 3 — 逐字表格对照（可选 UI）

- [ ] `TranslationTableView` 双列单行 ScrollView
- [ ] 与自由编辑区 Toggle 切换

### Phase 4 — 阅读模式对照翻页

- [ ] 译文 `BookPaginator` + 页映射表
- [ ] `ReadingSpreadView` 右页译文渲染
- [ ] 「对照翻页」Toggle + spread 联动
- [ ] 学习模式 ↔ 阅读模式位置互跳

---

## 11. 文件改动一览

| 文件 | 改动 |
|------|------|
| `ReadingViewModel.swift` | alignment 状态、生成管道、stale 检测 |
| `SelectableTextView.swift` | scroll 回调、scrollToRange、highlight |
| `ExplanationChatView.swift` | 同步开关、表格对照视图 |
| `ContentView.swift` | 左页 scroll 注册 |
| `ChatSessionStore.swift` | 持久化 alignment |
| `ReadingSpreadView.swift` | 对照翻页右页 |
| `BookPaginator.swift` | 按 block 边界分页 helper（可选） |
| 新增 `Sources/AIBook/Reading/Translation/*` | 见 §6.1 |

---

## 12. 测试要点

| 场景 | 预期 |
|------|------|
| 整段翻译 5 段 | 5 个 block，sourceRange 连续覆盖 |
| 左页滚到第 3 段 | 右页滚到第 3 段译文顶部 |
| 右页滚到第 2 段 | 左页高亮第 2 段原文 |
| 关闭同步滚动 | 左右独立滚动 |
| 改原文一字 | stale 横幅，同步暂停 |
| 逐字 100+ 行 | 锚定率 ≥ 90%，滚动不卡顿 |
| 对照翻页 ON | 翻 spread 后左右页码语义一致 |
| 重启 App | alignment 与 scroll 开关恢复 |

---

## 13. 风险与对策

| 风险 | 对策 |
|------|------|
| LLM 不按 JSON 输出 | 回退 `TranslationLineParser` + stale 标记 |
| 双 ScrollView 反馈环 | `isPropagating` 标志 |
| IME 输入时 scroll 抖动 | 组合文字期间暂停 sync |
| 译文页与原文页高度差大 | 只用块锚点，不用比例 |
| 块锚定失败 | 块仍显示译文，同步时跳过未锚定块 |

---

## 14. 与现有文档关系

- **学习模式布局**：不变（左原文 + 右 AI）；同步滚动为翻译 Tab 增强。
- **阅读模式**：见 [aibook阅读模式.md](./aibook阅读模式.md)；对照翻页为可选叠加。
- **讲解 Tab**：不在本设计范围。

---

**文档版本**：v1.0 · 2026-09-09
